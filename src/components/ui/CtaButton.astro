---
interface Props {
  href: string;
  icon?: string;
  variant?: "primary" | "secondary";
  size?: "md" | "lg";
  class?: string;
}

const {
  href,
  icon = "↗",
  variant = "primary",
  size = "md",
  class: className,
} = Astro.props;

const sizeClasses = size === "lg" ? "min-h-14 px-6" : "min-h-12 px-5";
// A softer, single-use version of --shadow-raised: that token is also worn by
// BackToTop's floating circular button, where the heavier drama reads fine —
// but on this wide CTA it looked like "too much" shadow, hence the lighter
// one-off value here instead of adjusting the shared token.
const variantClasses =
  variant === "primary"
    ? "bg-raise shadow-[0_6px_16px_rgba(0,0,0,0.1),inset_0_1px_0_var(--color-raise-line)] text-text"
    : "text-muted bg-transparent";
---

<a
  href={href}
  class:list={[
    "inline-flex items-center justify-center rounded-xl border border-border text-sm font-bold transition duration-200 hover:-translate-y-0.5",
    sizeClasses,
    variantClasses,
    className,
  ]}
>
  <slot />
  <span class="text-blue ml-2">{icon}</span>
</a>
